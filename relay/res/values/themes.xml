<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Translucent overlay for the inbox detail page: the note app stays
         visible underneath while Markdown/LaTeX is rendered on top. -->
    <style name="Theme.Inkling.Translucent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
